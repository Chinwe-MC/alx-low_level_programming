#include "main.h"
#include <string.h>

/**
 *_strlen_recursion - gets length of an int
 *@s: function parameter
 *Return: integer
 */

int _strlen_recursion(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}

